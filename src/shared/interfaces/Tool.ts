// represents a Tool to be displayed on the site
export interface Tool {
    // The name of the Tool
    name: string;
    // a short description of the Tool to be displayed
    description: string;
    // that path to the image
    imagePath: string
}